= content_for :title do
  = t('.edit_profile')
= content_for :subtitle do
  = raw(t('.edit_profile_description'))
= form_for(resource,
  as: resource_name,
  builder: BulmaFormBuilder,
  url: registration_path(resource_name),
  html: { method: :put }) do |f|
  = devise_error_messages!
  = render 'tabs'
  #personal-form
    = render 'edit_form', f: f
  #address-form{ style: "display: none;" }
    = render 'address', resource: resource, f: f
  %br/
  .actions
    = f.submit t('.update')
%br/
%br/
%h3= t('.cancel_my_account')
%p
  = t('.unhappy')
  = button_to t('.cancel_my_account'), registration_path(resource_name), data: { confirm: t('.are_you_sure') }, method: :delete, class: 'button'
= link_to t('devise.shared.links.back'), :back, class: 'button'
